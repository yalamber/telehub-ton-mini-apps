/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: 'bh2mhrlpbfsy8sv3.public.blob.vercel-storage.com' },
    ],
  },
};

export default nextConfig;
